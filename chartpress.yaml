charts:
  - name: helm-chart/renku-notebooks
    resetTag: latest
    imagePrefix: renku/
    repo:
      git: SwissDataScienceCenter/helm-charts
      published: https://swissdatasciencecenter.github.io/helm-charts
    paths:
      - .
      - jupyterhub
      - singleuser
      - clean_user_registry_secrets
    images:
      renku-notebooks:
        contextPath: .
        dockerfilePath: Dockerfile
        valuesPath: image
      jupyterhub-k8s:
        contextPath: jupyterhub
        dockerfilePath: jupyterhub/Dockerfile
        valuesPath: jupyterhub.hub.image
        paths:
          - jupyterhub
      git-clone:
        contextPath: git-clone
        dockerfilePath: git-clone/Dockerfile
        valuesPath: git_clone.image
        paths:
          - git-clone
      clean_user_registry_secrets:
        contextPath: clean_user_registry_secrets
        dockerfilePath: clean_user_registry_secrets/Dockerfile
        valuesPath: clean_user_registry_secrets.image
        paths:
          - clean_user_registry_secrets
