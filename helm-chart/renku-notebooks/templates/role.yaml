{{ if .Values.rbac.create }}
{{- $namespaces := list -}}
{{ if .Values.sessionsNamespace }}
{{- $namespaces = list .Release.Namespace .Values.sessionsNamespace | uniq -}}
{{ else }}
{{- $namespaces = list .Release.Namespace -}}
{{ end }}
{{ range $namespaces }}
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: {{ template "notebooks.fullname" $ }}
  namespace: {{ . }}
  labels:
    app: {{ template "notebooks.name" $ }}
    chart: {{ template "notebooks.chart" $ }}
    release: {{ $.Release.Name }}
    heritage: {{ $.Release.Service }}
rules:
  - apiGroups:
      - ""
    resources:
      - pods
      - pods/log
      - services
      - endpoints
      - secrets
    verbs:
      - get
      - list
      - watch
  - apiGroups:
      - ""
    resources:
      - pods
      - secrets
    verbs:
      - delete
  - apiGroups:
      - ""
    resources:
      - secrets
    verbs:
      - create
      - update
      - delete
      - patch
  - apiGroups:
      - {{ $.Values.amalthea.crdApiGroup }}
    resources:
      - {{ $.Values.amalthea.crdNames.plural }}
    verbs:
      - create
      - update
      - delete
      - patch
      - list
      - get
      - watch
{{ end }}
{{ end }}
